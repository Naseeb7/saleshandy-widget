import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Navbar } from "@/components/layout/navbar";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL(
    process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000",
  ),
  title: {
    default: "Saleshandy Testimonials",
    template: "%s | Saleshandy Testimonials",
  },
  description:
    "Share, review, and discover customer testimonials with Saleshandy.",
  keywords: ["Saleshandy", "customer testimonials", "testimonials"],
  alternates: {
    canonical: "/",
  },
  openGraph: {
    type: "website",
    locale: "en_US",
    url: "/",
    siteName: "Saleshandy Testimonials",
    title: "Saleshandy Testimonials",
    description:
      "Share, review, and discover customer testimonials with Saleshandy.",
  },
  twitter: {
    card: "summary",
    title: "Saleshandy Testimonials",
    description:
      "Share, review, and discover customer testimonials with Saleshandy.",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Navbar />
        {children}
      </body>
    </html>
  );
}
